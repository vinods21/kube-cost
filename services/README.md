# Services

Each child directory is an independently buildable process boundary. Most skeleton binaries expose only the standard gRPC health service; `allocation` exposes the V1 HTTP namespace-cost API.
