# Kubernetes Agent

The agent currently starts an empty controller-runtime manager with health probes and leader election. Collectors, caches, WAL, and transport behavior are intentionally not implemented.
