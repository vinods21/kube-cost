# Protocol Buffers

Source contracts are organized by versioned package under `cost/v1`. Generated Go files belong in `proto/gen/go` and are excluded from handwritten service startup until implementations are introduced.

Generate contracts with `make proto` or `scripts/generate-proto.ps1`.
